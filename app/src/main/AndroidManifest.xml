<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/dinero"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/dinero"
        android:supportsRtl="true"
        android:theme="@style/Theme.TuCash"
        tools:targetApi="31">
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".LoginActivity" />

        <!-- ForgotPasswordActivity: La actividad para recuperar la contraseña -->
        <activity android:name=".ForgotPasswordActivity" />

        <!-- RegisterActivity: La actividad para registrar nuevos usuarios -->
        <activity android:name=".RegisterActivity" />

    </application>


</manifest>